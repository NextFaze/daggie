package com.nextfaze.daggie.moshi

import android.app.Application

/** Bare-bones `Application` used to avoid running all of the production code that typically starts with the app. */
class RobolectricApplication : Application()
